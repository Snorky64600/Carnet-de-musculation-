import 'package:flutter/material.dart';

class OPTIONS_OU_AUTRE_Screen extends StatelessWidget { // Change le nom pour chaque fichier (ex: OptionsScreen)
  const OPTIONS_OU_AUTRE_Screen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Écran en cours de création')),
      body: const Center(child: Text('En attente de modularisation')),
    );
  }
}
